language: python
python:
  - 2.7

install:
  - pip install ansible

script:
  - ./ansible-runner localhost test/tasks.yml -- -c local
  - ./ansible-runner localhost test/role -- -c local
  - ./ansible-runner localhost test/playbook.yml -- -c local